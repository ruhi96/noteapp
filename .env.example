# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key
SUPABASE_STORAGE_ENDPOINT=https://your-project.storage.supabase.co/storage/v1/s3

# Firebase Backend Configuration (Required for Admin SDK)
# Firebase Admin SDK - Service account JSON as single-line string
# Get from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Firebase Frontend Configuration (Required for Google Sign-in Authentication)
# Get these from: Firebase Console > Project Settings > Your apps > Web app
# Only these 3 variables are required for Firebase Authentication:
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id

# Note: Storage, Messaging, and App ID are NOT required for authentication only

# Server Configuration
PORT=3001

